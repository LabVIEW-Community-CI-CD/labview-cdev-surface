name: release-control-plane

on:
  schedule:
    - cron: '20 6 * * *'
  workflow_dispatch:
    inputs:
      mode:
        description: Control-plane mode.
        required: false
        default: FullCycle
        type: choice
        options:
          - Validate
          - CanaryCycle
          - PromotePrerelease
          - PromoteStable
          - FullCycle
      sync_guard_max_age_hours:
        description: Maximum age in hours for latest successful sync-guard run.
        required: false
        default: '12'
        type: string
      auto_remediate:
        description: Auto-remediate ops drift before release dispatch.
        required: false
        default: true
        type: boolean
      keep_latest_canary_n:
        description: Number of latest SemVer canary tags to keep.
        required: false
        default: '1'
        type: string
      force_stable_promotion_outside_window:
        description: Force stable promotion outside policy window for FullCycle (audited).
        required: false
        default: false
        type: boolean
      force_stable_promotion_reason:
        description: Audit reason when forcing stable promotion outside policy window.
        required: false
        default: ''
        type: string
      dry_run:
        description: Run planning and health gates only; do not dispatch releases.
        required: false
        default: false
        type: boolean

permissions:
  contents: write
  actions: write
  issues: write

concurrency:
  group: release-control-plane-${{ github.repository }}-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  release-control-plane:
    name: Release Control Plane
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Enforce hosted-runner lock
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $runnerEnvironment = [string]$env:RUNNER_ENVIRONMENT
          if ($runnerEnvironment -ne 'github-hosted') {
            throw "hosted_runner_required: release-control-plane must run on a github-hosted runner. actual='$runnerEnvironment'"
          }

      - name: Execute autonomous release control plane
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $ErrorActionPreference = 'Stop'
          $reportPath = Join-Path $env:RUNNER_TEMP 'release-control-plane-report.json'
          $overrideAuditReportPath = Join-Path $env:RUNNER_TEMP 'release-control-plane-override-audit.json'

          $mode = [string]'${{ inputs.mode }}'
          if ([string]::IsNullOrWhiteSpace($mode)) {
            $mode = 'FullCycle'
          }

          $syncGuardAgeHoursText = [string]'${{ inputs.sync_guard_max_age_hours }}'
          $syncGuardAgeHours = 12
          if (-not [string]::IsNullOrWhiteSpace($syncGuardAgeHoursText)) {
            $parsedSyncGuardAgeHours = 0
            if (-not [int]::TryParse($syncGuardAgeHoursText, [ref]$parsedSyncGuardAgeHours)) {
              throw "sync_guard_max_age_hours must be an integer. actual='$syncGuardAgeHoursText'"
            }
            $syncGuardAgeHours = $parsedSyncGuardAgeHours
          }

          $keepLatestCanaryNText = [string]'${{ inputs.keep_latest_canary_n }}'
          $keepLatestCanaryN = 1
          if (-not [string]::IsNullOrWhiteSpace($keepLatestCanaryNText)) {
            $parsedKeepLatestCanaryN = 0
            if (-not [int]::TryParse($keepLatestCanaryNText, [ref]$parsedKeepLatestCanaryN)) {
              throw "keep_latest_canary_n must be an integer. actual='$keepLatestCanaryNText'"
            }
            $keepLatestCanaryN = $parsedKeepLatestCanaryN
          }

          $autoRemediateText = [string]'${{ inputs.auto_remediate }}'
          $autoRemediate = $true
          if (-not [string]::IsNullOrWhiteSpace($autoRemediateText)) {
            $autoRemediate = [System.Convert]::ToBoolean($autoRemediateText)
          }

          $dryRunText = [string]'${{ inputs.dry_run }}'
          $dryRun = $false
          if (-not [string]::IsNullOrWhiteSpace($dryRunText)) {
            $dryRun = [System.Convert]::ToBoolean($dryRunText)
          }

          $forceStablePromotionOutsideWindowText = [string]'${{ inputs.force_stable_promotion_outside_window }}'
          $forceStablePromotionOutsideWindow = $false
          if (-not [string]::IsNullOrWhiteSpace($forceStablePromotionOutsideWindowText)) {
            $forceStablePromotionOutsideWindow = [System.Convert]::ToBoolean($forceStablePromotionOutsideWindowText)
          }

          $forceStablePromotionReason = [string]'${{ inputs.force_stable_promotion_reason }}'
          if ($null -eq $forceStablePromotionReason) {
            $forceStablePromotionReason = ''
          }

          & pwsh -NoProfile -File ./scripts/Invoke-ReleaseControlPlane.ps1 `
            -Repository '${{ github.repository }}' `
            -Branch 'main' `
            -Mode $mode `
            -SyncGuardMaxAgeHours $syncGuardAgeHours `
            -KeepLatestCanaryN $keepLatestCanaryN `
            -AutoRemediate:$autoRemediate `
            -ForceStablePromotionOutsideWindow:$forceStablePromotionOutsideWindow `
            -ForceStablePromotionReason $forceStablePromotionReason `
            -DryRun:$dryRun `
            -OverrideAuditOutputPath $overrideAuditReportPath `
            -OutputPath $reportPath

      - name: Upload release control plane report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: release-control-plane-report-${{ github.run_id }}
          path: ${{ runner.temp }}/release-control-plane-report.json
          if-no-files-found: error

      - name: Upload release control plane stable override audit
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: release-control-plane-override-audit-${{ github.run_id }}
          path: ${{ runner.temp }}/release-control-plane-override-audit.json
          if-no-files-found: error

      - name: Open stable override incident issue when out-of-window override is applied
        if: always()
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
          RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          $ErrorActionPreference = 'Stop'
          $reportPath = Join-Path $env:RUNNER_TEMP 'release-control-plane-report.json'
          $auditPath = Join-Path $env:RUNNER_TEMP 'release-control-plane-override-audit.json'
          if (-not (Test-Path -LiteralPath $reportPath -PathType Leaf)) {
            throw "Release control plane report missing: $reportPath"
          }
          if (-not (Test-Path -LiteralPath $auditPath -PathType Leaf)) {
            throw "Release control plane override audit report missing: $auditPath"
          }

          $report = Get-Content -LiteralPath $reportPath -Raw | ConvertFrom-Json -ErrorAction Stop
          $audit = Get-Content -LiteralPath $auditPath -Raw | ConvertFrom-Json -ErrorAction Stop
          $decisionReason = [string]$report.stable_promotion_window.decision.reason_code
          if ($decisionReason -ne 'stable_window_override_applied') {
            Write-Host "No out-of-window stable override applied. decision_reason_code='$decisionReason'"
            exit 0
          }

          $allowedWeekdays = [string]::Join(',', @($report.stable_promotion_window.full_cycle_allowed_utc_weekdays))
          $title = 'Release Control Plane Stable Override Alert'
          $body = @"
          Out-of-window stable override was applied.

          - Run: $env:RUN_URL
          - Mode: $($report.mode)
          - Decision: $decisionReason
          - Current UTC weekday: $($report.stable_promotion_window.decision.current_utc_weekday)
          - Allowed UTC weekdays: $allowedWeekdays
          - Override reference: $($audit.override_reference)
          - Override reason: $($report.stable_promotion_window.override_reason)
          - Stable target tag: $($audit.stable_target_tag)
          - Stable release: $($audit.stable_release_url)
          "@

          & pwsh -NoProfile -File ./scripts/Invoke-OpsIncidentLifecycle.ps1 `
            -Repository $env:REPOSITORY `
            -IssueTitle $title `
            -Mode Fail `
            -RunUrl $env:RUN_URL `
            -Body $body

      - name: Update release control plane incident issue on failure
        if: failure()
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
          RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          $ErrorActionPreference = 'Stop'
          $title = 'Release Control Plane Alert'
          $reportPath = Join-Path $env:RUNNER_TEMP 'release-control-plane-report.json'
          if (-not (Test-Path -LiteralPath $reportPath -PathType Leaf)) {
            throw "Release control plane report missing: $reportPath"
          }

          $report = Get-Content -LiteralPath $reportPath -Raw | ConvertFrom-Json -ErrorAction Stop
          $body = @"
          Release control plane failed.

          - Run: $env:RUN_URL
          - Mode: $($report.mode)
          - Reason code: $($report.reason_code)
          - Message: $($report.message)
          - Repository: $($report.repository)
          "@

          & pwsh -NoProfile -File ./scripts/Invoke-OpsIncidentLifecycle.ps1 `
            -Repository $env:REPOSITORY `
            -IssueTitle $title `
            -Mode Fail `
            -RunUrl $env:RUN_URL `
            -Body $body

      - name: Close release control plane incident issue on recovery
        if: success()
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
          RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          $ErrorActionPreference = 'Stop'
          $title = 'Release Control Plane Alert'
          $reportPath = Join-Path $env:RUNNER_TEMP 'release-control-plane-report.json'
          if (-not (Test-Path -LiteralPath $reportPath -PathType Leaf)) {
            throw "Release control plane report missing: $reportPath"
          }

          $report = Get-Content -LiteralPath $reportPath -Raw | ConvertFrom-Json -ErrorAction Stop
          $body = @"
          Release control plane recovered.

          - Run: $env:RUN_URL
          - Mode: $($report.mode)
          - Reason code: $($report.reason_code)
          - Message: $($report.message)
          - Repository: $($report.repository)
          "@

          & pwsh -NoProfile -File ./scripts/Invoke-OpsIncidentLifecycle.ps1 `
            -Repository $env:REPOSITORY `
            -IssueTitle $title `
            -Mode Recover `
            -RunUrl $env:RUN_URL `
            -Body $body
